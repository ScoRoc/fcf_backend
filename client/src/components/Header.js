import React from 'react';

import Nav from './Nav';

const Header = props => {
  return (
    <header>
      <h2>FCF Management Portal</h2>
      <Nav />
    </header>
  );
}

export default Header;
