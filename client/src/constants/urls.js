const URL = {
  ANNOUNCEMENTS: '/announcements',
  AUTH: '/auth',
  EVENTS: '/events',
  LOGIN: '/login',
  ROOT: '/',
  USERS: '/users',
  WODS: '/wods',
};

const QUERY_STRING = {
  LOGIN_FROM: 'portal',
};

export { QUERY_STRING, URL };
