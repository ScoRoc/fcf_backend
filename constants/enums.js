const LAST_LOGIN = Object.freeze({
  APP: 'app',
  PORTAL: 'portal',
});

const ROLES = Object.freeze({
  ADMIN: 'admin',
  SUPER_ADMIN: 'super-admin',
  USER: 'user',
});

module.exports = {
  LAST_LOGIN,
  ROLES,
};