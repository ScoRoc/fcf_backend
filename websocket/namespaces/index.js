const { makeAnnouncementsNamespace } = require('./announcements');
const { makeEventsNamespace } = require('./events');
const { makeWodsNamespace } = require('./wods');

module.exports = {
  makeAnnouncementsNamespace,
  makeEventsNamespace,
  makeWodsNamespace,
};
